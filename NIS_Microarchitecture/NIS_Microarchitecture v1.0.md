# NIS_Microarchitecture Main v1.0
\``mermaid`
flowchart TD
    %% ==================== 外部接口与缓冲层 ====================
    A[通讯接口等...] --> B[FIFO取指端]
    B --> C[中央路由]

    %% ==================== 流水线主干 ====================
    C --> D[取指]
    D --> E[路由]
    E --> F[解码]

    %% ==================== 双执行通道（解码后分发） ====================
    F --> G[执行<br/>并行运算]
    F --> H[执行<br/>逻辑运算]

    %% ==================== 指令位宽-字节映射（图顶标注） ====================
    subgraph 指令位宽映射
        direction LR
        I1[16位] --- J1[2]
        I2[24位] --- J2[3]
        I3[32位] --- J3[4]
        I4[48位] --- J4[6]
    end

    %% ==================== 缓冲容量标注（左上角） ====================
    K[指令缓冲<br/>64字节]
\```